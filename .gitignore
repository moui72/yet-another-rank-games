# Dependencies
node_modules/

# SvelteKit / Vite build output
.svelte-kit/
build/
/package/

# Test / coverage output
coverage/
.nyc_output/
test-results/
playwright-report/
/playwright/.cache/

# Environment / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log

# ARDD install (reinstalled via install.sh; copied into worktrees via .worktreeinclude)
.claude/

# ARDD advisory project lock (ephemeral)
.project/.lock

*storybook.log
storybook-static
